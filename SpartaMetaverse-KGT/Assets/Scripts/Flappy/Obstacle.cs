using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Obstacle : MonoBehaviour
{
    private float highPosition = 1f;  // 장애물 높이 위치
    private float lowPosition = -1f; // 장애물 낮은 위치

    private float holeSizeMin = 1f; // 장애물 사이 구멍 최소 크기
    private float holeSizeMax = 3f; // 장애물 사이 구멍 최대 크기


    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
